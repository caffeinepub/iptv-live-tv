{
  "frontend/index.html": "HTML entry point with updated title, Google Fonts (Inter + Barlow Condensed), and hls.js CDN script for HLS stream playback.",
  "frontend/src/index.css": "Dark cinematic OKLCH design system with deep navy backgrounds, vivid red primary, amber secondary accent, scrollbar-hide utility, card glow, nav blur, and pulse-red animation.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/Barlow Condensed fonts, dark cinematic color palette, glow box shadows, and pulse-red animation keyframes.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for channels, favourites, user profile, and playlists — includes useGetMyPlaylists, useCreatePlaylist, useDeletePlaylist, useAddChannelToPlaylist, and useRemoveChannelFromPlaylist mutations with proper query invalidation.",
  "frontend/src/App.tsx": "Root app component that renders the ChannelBrowser page.",
  "frontend/src/components/Navigation.tsx": "Sticky navigation bar with logo, scrollable filter tabs (All Channels, Favourites with star icon that fills when active, per-playlist tabs with delete ×), a New Playlist button for authenticated users, and sign-in/sign-out auth controls; renders CreatePlaylistModal.",
  "frontend/src/components/VideoPlayer.tsx": "HLS-compatible video player for backend Channel objects with fullscreen support, an auto-hiding overlay (fades out after 3 s of inactivity, reappears on interaction), and a star favourite toggle button visible in the non-fullscreen top-right controls, the fullscreen overlay, and the channel info bar; accepts `isFavourite` and `onToggleFavourite` props.",
  "frontend/src/components/ChannelCard.tsx": "Backend channel card with thumbnail, live badge, selected state, favourite heart toggle, and an AddToPlaylistPopover button shown only to authenticated users.",
  "frontend/src/components/FilterControls.tsx": "Filter bar with a search input (with clear button) above language and country Select dropdowns, a clear-filters button, and a channel count display; accepts `searchQuery` and `onSearchChange` props.",
  "frontend/src/components/ChannelGrid.tsx": "Responsive channel grid with skeleton loading states and empty state illustration.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time login users to set their display name before accessing the app.",
  "frontend/src/pages/ChannelBrowser.tsx": "Main page that merges M3U and manual channels, manages filter/search/favourites state, shows a favourites count banner when in Favourites view, handles smart deselection when unfavouriting the active channel in Favourites mode, and passes `handleToggleFavourite` to both the video player and channel grid.",
  "frontend/src/utils/m3uParser.ts": "Utility module that parses M3U playlist text into an array of M3UChannel objects, extracting name, stream URL, logo, language, and country from #EXTINF entries.",
  "frontend/src/hooks/useM3UChannels.ts": "React Query hook that fetches an M3U playlist from a given URL (with automatic CORS proxy fallback via corsproxy.io) and returns parsed M3UChannel array with loading/error states.",
  "frontend/src/hooks/useLocalFavourites.ts": "Custom hook that manages a set of favourite channel IDs persisted in localStorage, providing toggleFavourite and isFavourite helpers.",
  "frontend/src/components/M3UChannelCard.tsx": "Channel card component for M3U-sourced channels with thumbnail, live badge, favourite heart toggle (no auth required), name, language and country badges, and selected state indicator.",
  "frontend/src/components/M3UChannelGrid.tsx": "Responsive grid component for M3U channels with skeleton loading states, empty state, and per-card favourite toggle callback.",
  "frontend/src/components/M3UVideoPlayer.tsx": "HLS-compatible video player for M3U channels with fullscreen support, an auto-hiding overlay (fades out after 3 s of inactivity, reappears on tap/click/mouse-move), and a star favourite toggle button visible both in the non-fullscreen header and in the fullscreen overlay; accepts `isFavourite` and `onToggleFavourite` props.",
  "frontend/src/hooks/useManualChannels.ts": "Custom hook that manages a session-only list of manually added M3U channels in React state, providing an `addChannel` function that accepts name, stream URL, and optional logo/language/country fields and returns the created channel object.",
  "frontend/src/components/AddChannelModal.tsx": "Modal dialog using shadcn/ui Dialog for adding individual IPTV channels manually, with form fields for channel name (required), stream URL (required), logo URL, language, and country (all optional), plus client-side validation and error display.",
  "frontend/src/components/CreatePlaylistModal.tsx": "Modal dialog for creating a new playlist with a name input, validation, and Create/Cancel buttons; calls the createPlaylist mutation and closes on success.",
  "frontend/src/components/AddToPlaylistPopover.tsx": "Popover component that lists all user playlists with toggle checkboxes for adding/removing a backend channel (by ChannelId) from each playlist; uses shadcn Popover and ScrollArea with dark cinematic styling."
}